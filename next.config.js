/** @type {import('next').NextConfig} */
const nextConfig = {
    // add image hostname
    images: {
        domains: ['upload.wikimedia.org','1000logos.net'],
        },
}

module.exports = nextConfig
